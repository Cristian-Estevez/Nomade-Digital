//el operador ternario
//simplifica el codigo

const autenticado = true;

console.log (autenticado ? 'si esta autenticado' : 'no esta autenticado');
// el ? seria el if, el : el else


//operador &&

const usuario = true;
const puedePagar = true;

console.log( usuario && puedePagar ? 'si puede pagar' : 'no puede pagar');


//operador ||
const cliente = true;
const puedeAbonar = true;

console.log( cliente || puedeAbonar ? 'si puede pagar' : 'no puede pagar');
