//ARREGLOS, permiten guardar info de elementos
//objeto, guarda variables, elemento y valor

const numeros = [1, 2, 3];

//otra forma de crear array,menos comun, como constructor
const meses = new Array('enero', 'febrero', 'marzo');

console.log(numeros);

//puede tener multiples tipos de elementos
const deTodo = ['hola', 1, 2.3, true, null, {nombre: 'juan', trabajo: 'programador'}, [4, 5, 6] ];
//tambien se puede crear un arreglo dentro de un arreglo
console.log(deTodo);
