//querySelectorAll
//seleccionar todos los elementos con la misma clase

const card = document.querySelectorAll('.card');
console.log(card);

//si el elemento no existe, retorna un node vacio
const noExiste = document.querySelectorAll('no-existe');
console.log(noExiste); //retorna un NULL


